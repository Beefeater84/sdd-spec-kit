# Context: #38 Remove roadmap and main leftovers from sdd-init-legacy

## Code map

- `.claude/commands/sdd-init-legacy.md` — the only copy of the command (no `.claude/skills/`, no distributable copy).
  - l.4: "use the standard SDD feature workflow" — fine.
  - Step 3 (l.25–97): "Create three files."; blocks for `specs/mission.md`, `specs/tech-stack.md`, `specs/roadmap.md` (l.82–97, remove).
  - Step 4 (l.99–106): question "Are there roadmap items missing or in the wrong order?" — replace (check AGENT.md topics; tasks live in GitHub Issues / project board).
  - Step 5 (l.108–112): "commit ... on its own branch (e.g. `chore/sdd-init`) before merging to main" — replace: own branch, PR into the working branch (`release/*` in the SDD flow); no `main`.
  - Step 6 (l.114–117): "...the next roadmap item" — replace with `/create-sdd-feature <issue>` for the next task from the board.
- `specs/AGENT.md` — the kit's own agent guide: "Always read" (mission, tech-stack, accepted ADRs via grep `^Status: accepted`), "Read by topic" table, "Feature folders", "Where decisions go". Sample for the AGENT.md block in Step 3; the topic table is to be filled from doc areas found in the project. Do not edit this file.
- `specs/decisions/.gitkeep` — the kit keeps an empty decisions folder this way.
- `README.md` l.29 — `/sdd-init-legacy` section: "reverse-engineers the three constitution files (`specs/mission.md`, `specs/tech-stack.md`, `specs/roadmap.md`)" — update to the new set. l.52, l.58 already describe AGENT.md; keep consistent.
- `specs/roadmap.md` — 13-line placeholder; delete.
- `.claude/commands/create-sdd-feature.md` l.9 "There is no roadmap file", l.11 "Never `main` or `master`", l.47 requires AGENT.md, mission.md, tech-stack.md.
- `agent.md` l.3–12 tasks in GitHub Issues/board; l.23–26 branches from/into latest `release/*`, never `main`/`master`, name `<type>/<issue>-<desc>`.

## Patterns to follow

- Generated-file block in Step 3 — like the existing `**`specs/mission.md`**` + fenced markdown block.
- Removal tasks (#13, #14) — change the command, fix README, leave history untouched.

## Conventions

- Do not edit historical docs: `docs/analysis/*`, `specs/features/*` (other than this folder).
- `.claude/worktrees/` is an untracked worktree on `main`; never edit it; exclude it from greps.
- Legit `main` mentions (releaser, "never target main" guards in agents) stay; this task only touches sdd-init-legacy, README l.29, roadmap.md.
- English, terse, imperative style in commands.
- No lint/typecheck/test tooling; checks are grep/git.

## Gaps found during implementation
