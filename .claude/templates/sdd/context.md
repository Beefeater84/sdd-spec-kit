# Context: #<id> <title>

<!-- For agents, not for the human. Dense: paths, symbols, signatures, short notes.
     Written by the orchestrator from the code research. Implementers read their slice of it
     instead of searching the codebase. -->

## Code map

- `<path>` — `<symbol>(<signature>)` — <what it does, how it matters for the task>

## Patterns to follow

- <what> — like `<path>`

## Conventions

- <rule that is easy to miss, e.g. "env is read only through `config.get`">

## Gaps found during implementation

<!-- Added by the orchestrator from implementers' `gaps`, so later groups do not search again. -->
